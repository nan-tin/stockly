Stocklyにお問い合わせが届きました

ユーザーID：
<%= @user.id %>

登録メールアドレス：
<%= @user.email %>

返信先メールアドレス：
<%= @inquiry.email %>

お問い合わせ種類：
<%= {
  "bug" => "不具合報告",
  "feature" => "機能要望",
  "other" => "その他"
}.fetch(@inquiry.inquiry_type) %>

送信日時：
<%= l(@inquiry.created_at, format: :long) %>

お問い合わせ内容：
<%= @inquiry.content %>

<% if @inquiry.image.attached? %>
添付画像：
<%= @inquiry.image.filename %>
<% end %>